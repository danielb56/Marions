import { NotificationList } from "@/components/notification-list";
import { createClient } from "@/lib/supabase/server";
export default async function WorkerNotificationsPage() { const supabase = await createClient(); const { data } = await supabase.from("notification").select("id,subject,body_redacted,action_url,channel,created_at,read_at").order("created_at", { ascending: false }).limit(100); return <><p className="text-xs font-bold uppercase tracking-[.14em] text-[#b44a00]">Inbox</p><h1 className="mb-5 mt-1 text-3xl font-semibold tracking-[-.04em]">Notifications</h1><NotificationList notifications={data ?? []} /></>; }
