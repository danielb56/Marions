import { expect, test } from "@playwright/test";
test("sign-in is secure and usable on desktop and mobile", async ({ page }) => { await page.goto("/sign-in"); await expect(page.getByRole("heading", { name: "Sign in to REME" })).toBeVisible(); await expect(page.getByRole("link", { name: "REME Painting Group home" })).toBeVisible(); await expect(page.getByText("No public sign-up")).toBeVisible(); await expect(page.locator("body")).not.toContainText("$2,200"); });
